version: "3.9"
services:
  nginxservice:
    image: 'dockertestimagenginx.my-nginx' 
    container_name: nginx
  deploy:
    replicas: 1
    restart_policy:
      condition: any
  ports:
    - "80:80"
